---
phase: "02-editor-annotazioni"
plan: "02-04"
type: "fix"
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/ui/workspace-home/DetailPane.tsx
  - src/ui/workspace-home/FullscreenOverlay.tsx
  - src/ui/workspace-home/ElementoEditor.tsx
  - src/ui/workspace-home/__tests__/display-helpers.test.ts
autonomous: true
gap_closure: true
requirements: ["R005"]
must_haves:
  truths:
    - "The real detail shell visually and behaviorally matches the canonical `UnifiedEditorMockup.tsx`: no separate action toolbar, header-integrated actions, metadata chips as the primary editing layer, lighter array sections, and a body-native `+ Aggiungi campo` flow."
    - "Pane and fullscreen present the same shell and editing grammar; fullscreen is an extension of the same unified detail experience, not a parallel variant."
    - "The realignment preserves the validated domain/session behavior from `02-03` (`editingFieldId`, session overrides, `normalizeElementoInput`) while keeping S03 concerns explicitly deferred."
  artifacts:
    - path: "src/ui/workspace-home/DetailPane.tsx"
      shape: "Stops rendering a standalone action toolbar above the detail body. The pane shell collapses into the unified header/body structure from the mockup and delegates editing affordances to the integrated editor shell."
    - path: "src/ui/workspace-home/FullscreenOverlay.tsx"
      shape: "Uses the same unified shell contract as the standard pane. Fullscreen chrome may differ, but the header structure, metadata row, section weight, and add-field flow remain the same."
    - path: "src/ui/workspace-home/ElementoEditor.tsx"
      shape: "Refactored from a functionally-correct but too form-like implementation into a closer production mapping of `UnifiedDetailPane`: header row, review badge/drawer, chip visuals, lighter array sections, and a body-native add-field entry. Interaction and composition mirror the mockup directly unless blocked by existing domain constraints."
    - path: "src/ui/workspace-home/__tests__/display-helpers.test.ts"
      shape: "Retains session-overlay coverage after the shell realignment; add/adjust tests only if the refactor changes helper call-sites or render-driving assumptions."
  key_links:
    - from: "src/ui/workspace-home/ElementoEditor.tsx::header shell"
      to: "src/ui/mockups/UnifiedEditorMockup.tsx::HeaderRow"
      via: "the real header must follow the same structure and affordance ordering as the mockup canon"
      pattern: "review|warning|titolo|metadata|add field"
    - from: "src/ui/workspace-home/ElementoEditor.tsx::section shells"
      to: "src/ui/mockups/UnifiedEditorMockup.tsx::RuoliSection|TagsSection|CollegamentiFamiliariSection|CollegamentiGenericiSection|AddFieldGlobalButton"
      via: "array sections and add-field flow should read as direct production mappings of the mockup structures"
      pattern: "rounded|chip|add|section"
---

<objective>
Close the visual/interaction gap left after `02-03`. The codebase now has the correct session/state and inline-editing foundation for `R005`, but the real UI still does not read like the canonical `UnifiedEditorMockup.tsx`. The current implementation is too form-like and preserves structural remnants the mockup explicitly removed: a standalone toolbar, heavier section shells, and a less integrated add-field experience.

This plan is a focused gap-closure pass for `R005`, not a new slice and not a domain rework. It exists to make the real detail shell feel like the mockup canon already chosen in `02-CONTEXT.md`. The comparison standard is strict: when the real code and `UnifiedEditorMockup.tsx` disagree on hierarchy, the mockup wins unless doing so would break the already-validated domain/session contract.

**Output:**
- A new executable fix plan that realigns the real workspace detail UI to the unified mockup.
- A concrete task sequence for shell simplification, metadata/body realignment, fullscreen parity, and regression verification.
- An implementation path that keeps `R005` inside S02 while explicitly leaving S03 concerns deferred.
</objective>

<execution_context>
@/home/fabio/dev/bible-board/.codex/get-shit-done/workflows/execute-plan.md
@/home/fabio/dev/bible-board/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-editor-annotazioni/02-CONTEXT.md
@.planning/phases/02-editor-annotazioni/02-03-PLAN.md
@.planning/phases/02-editor-annotazioni/02-03-SUMMARY.md
@src/ui/mockups/UnifiedEditorMockup.tsx
@src/ui/workspace-home/DetailPane.tsx
@src/ui/workspace-home/FullscreenOverlay.tsx
@src/ui/workspace-home/ElementoEditor.tsx
@src/ui/workspace-home/workspace-ui-store.ts
@src/ui/workspace-home/display-helpers.ts
@src/ui/workspace-home/__tests__/display-helpers.test.ts
@.heroui-docs/react/components/(collections)/dropdown.mdx
@.heroui-docs/react/components/(overlays)/popover.mdx
@.heroui-docs/react/components/(overlays)/drawer.mdx
@.heroui-docs/react/components/(forms)/text-field.mdx

## Locked decisions from context

- `src/ui/mockups/UnifiedEditorMockup.tsx` remains the canonical visual/interaction reference for `R005`.
- The realignment must remove the standalone toolbar and restore the mockup’s integrated header grammar.
- Metadata chips, array sections, and `+ Aggiungi campo` must be judged against the mockup’s hierarchy, not only against functional correctness.
- S03 scope stays out: no broad fonti/catalog editing is allowed in this fix pass.
- The domain/session contract from `02-03` is already the correct foundation and should be preserved, not redesigned.

## Constraints

- Do not undo `editingFieldId`, session overrides, or `normalizeElementoInput`-driven commits.
- Do not mutate `src/mock/data.ts`; visible edits still come from session overlays only.
- Keep fullscreen and pane variants on the same shell contract.
- Use local HeroUI v3 docs for composition details; do not improvise undocumented anatomy.
</context>

<tasks>

<task id="T01" type="auto" estimate="75min">
  <name>Collapse the detail shell onto the unified mockup structure</name>
  <files>
    src/ui/workspace-home/DetailPane.tsx,
    src/ui/workspace-home/FullscreenOverlay.tsx,
    src/ui/workspace-home/ElementoEditor.tsx
  </files>

  <behavior>
    - The detail view no longer renders a separate toolbar between header chrome and content.
    - The top of the editor reads like the mockup: inline title, review badge, kebab actions, then metadata chips.
    - Fullscreen keeps its navigation affordances but reuses the same shell grammar as the pane.
  </behavior>

  <action>
1. Remove the standalone `ActionToolbar` from the active detail experience:
   - Either delete it from the main flow or reduce it to a helper that is no longer shown in pane/fullscreen.
   - The interactive controls it carried must be redistributed into the unified header/body shell instead of being preserved as a separate bar.

2. Refactor `ElementoEditor.tsx` so the real shell maps directly to the mockup’s `HeaderRow`:
   - inline title editor,
   - warning/review badge and drawer,
   - kebab actions in the same header cluster.

3. Align `DetailPane.tsx` and `FullscreenOverlay.tsx` with the new shell:
   - both should mount the same unified shell,
   - fullscreen-specific navigation controls stay outside the content shell,
   - the content structure itself must not fork.
  </action>

  <verify>
    - No separate action toolbar is visible above the detail shell in either pane or fullscreen mode.
    - The first visible block of the real editor matches the mockup’s header order and feel.
    - `npx tsc --noEmit`
  </verify>
</task>

<task id="T02" type="auto" estimate="120min">
  <name>Realign metadata chips and section weight to the production mockup mapping</name>
  <files>
    src/ui/workspace-home/ElementoEditor.tsx
  </files>

  <behavior>
    - Metadata chips read like direct production versions of `TipoDropdownChip`, `VitaDrawerChip`, and `MetaChipText`.
    - Array sections (`ruoli`, `tags`, `collegamenti`) feel light and inline, not like heavy card/form blocks.
    - The body hierarchy emphasizes descrizione first, then lightweight arrays/links, matching the mockup’s rhythm.
  </behavior>

  <action>
1. Compare the production chip visuals and interaction wrappers against the mockup implementations:
   - align spacing, affordance ordering, and visual emphasis,
   - remove unnecessary extra wrappers that make the UI read as a form instead of a detail surface.

2. Simplify the array/link sections:
   - prefer the mockup’s lighter section shell over heavier card-like containers,
   - keep remove/add affordances local to the chips/section header,
   - preserve existing session patch behavior while changing the presentation.

3. Ensure descrizione remains the main body entry and the rest of the sections follow with the same hierarchy the mockup uses.
  </action>

  <verify>
    - Metadata chips look and behave as the first editing layer, not as secondary form controls.
    - Array sections are visually lighter and closer to the mockup’s chip-strip pattern.
    - `npx tsc --noEmit`
  </verify>
</task>

<task id="T03" type="auto" estimate="90min">
  <name>Make + Aggiungi campo and deferred S03 messaging match the canonical body flow</name>
  <files>
    src/ui/workspace-home/ElementoEditor.tsx,
    src/ui/workspace-home/DetailPane.tsx,
    src/ui/workspace-home/FullscreenOverlay.tsx
  </files>

  <behavior>
    - `+ Aggiungi campo` feels like part of the body’s editing grammar, not a detached utility panel.
    - Empty/secondary fields surface through the same data-driven logic, but presented in the mockup’s tone.
    - S03 exclusions remain visible yet low-noise, not promoted into feature work.
  </behavior>

  <action>
1. Rework the current add-field block to mirror the mockup’s `AddFieldGlobalButton` intent:
   - closer placement in the body flow,
   - lighter framing,
   - options expressed as supported S02 additions only.

2. Keep S03 messaging explicit but understated:
   - show deferred state where needed,
   - do not let deferred fonti/catalog affordances dominate the interaction.

3. Confirm the same flow exists in pane and fullscreen without a second implementation style.
  </action>

  <verify>
    - `+ Aggiungi campo` reads as a native part of the mockup shell.
    - S03-only editing remains deferred and does not pollute the core flow.
    - `npx tsc --noEmit`
  </verify>
</task>

<task id="T04" type="auto" tdd="true" estimate="60min">
  <name>Re-run regression coverage and capture mockup parity checks</name>
  <files>
    src/ui/workspace-home/__tests__/display-helpers.test.ts
  </files>

  <behavior>
    - Existing merged-session helper coverage remains green after the shell realignment.
    - Manual verification explicitly checks visual/interaction parity against the mockup, not only correctness of data writes.
  </behavior>

  <action>
1. Keep `display-helpers.test.ts` green; add or adjust tests only if the UI refactor changes assumptions about resolved session data.

2. Run project validation:
   - `npx tsc --noEmit`
   - `npx vitest run`

3. Capture a manual parity checklist for the executor summary:
   - no separate toolbar,
   - header matches mockup hierarchy,
   - metadata chips are the primary control layer,
   - sections feel lightweight/integrated,
   - `+ Aggiungi campo` feels body-native,
   - fullscreen matches the same shell,
   - annotations + soft delete still behave,
   - S03 stays deferred.
  </action>

  <verify>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0.
    - The executor summary can evaluate the implementation against explicit mockup-parity checks, not just functional checks.
  </verify>
</task>

</tasks>

<verification>

## Automated

- `npx tsc --noEmit`
- `npx vitest run`

## Manual

- Open the same element in pane and fullscreen and confirm the content shell is the same structure in both places.
- Confirm there is no standalone toolbar between the header chrome and the detail content.
- Compare the real header against `UnifiedEditorMockup.tsx`: title, review badge, kebab actions, metadata chips.
- Confirm `ruoli`, `tags`, and `collegamenti` read as light inline sections rather than heavy forms/cards.
- Confirm `+ Aggiungi campo` feels integrated into the body flow and only exposes S02-supported additions.
- Confirm annotations remain visible and soft delete still exposes the 30-second undo flow.

</verification>

<success_criteria>

- The plan clearly identifies this as a gap-closure realignment after `02-03`, not a duplicate restatement of the original R005 plan.
- The plan explicitly removes the separate toolbar and restores the mockup’s integrated shell hierarchy.
- The plan preserves the `02-03` domain/session foundation while correcting visual and interaction divergence.
- The executor can use `UnifiedEditorMockup.tsx` as a direct parity checklist while keeping S03 scope out.

</success_criteria>
