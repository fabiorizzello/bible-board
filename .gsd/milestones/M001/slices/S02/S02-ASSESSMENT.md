---
sliceId: S02
uatType: artifact-driven
verdict: PARTIAL
date: 2026-04-22T13:58:30.000Z
---

# UAT Result — S02

Artifact-driven inspection of the post-slice codebase at `/home/fabio/dev/bible-board` against `.gsd/milestones/M001/slices/S02/S02-UAT.md`. Evidence gathered via `Read`/`Grep` on the slice files listed in `S02-SUMMARY.md`, plus full test + lint runs from the project root.

## Build & test evidence

- `pnpm run lint` → exit 0 (`tsc -b --noEmit`, no diagnostics).
- `pnpm test -- --run` → **78 tests passing** in 3 real test files (`src/features/elemento/__tests__/elemento.rules.test.ts`, `src/ui/workspace-home/__tests__/display-helpers.test.ts`, `src/mock/__tests__/data.test.ts`). The 3 "Failed Suites" reported are `.gsd/worktrees/M001-S02/**` residues — the worktree directory is gone on disk, vitest discovers stale paths and errors with `Cannot find module '/@fs/home/fabio/.gsd/projects/99f42a6c959a/worktrees/M001-S02/…'`. This matches the S02-SUMMARY follow-up about worktree cleanup and does NOT reflect a real test failure.

## Checks

| Check | Mode | Result | Notes |
|-------|------|--------|-------|
| UC1 — Inline edit scalare (Titolo): blur-to-save + toast 5s + Annulla | artifact | PASS | `InlineTitle` (ElementoEditor.tsx:905–962) wires `onBlur={() => onCommit(draft)}`, Enter commits, Esc cancels. `commitTitle` (L450) routes through `commitPatch` which issues `toast("Titolo aggiornato", { timeout: 5_000, actionProps: { children: "Annulla", onPress: <rollback via normalizeElementoInput+commitNormalizedElement on prevElement snapshot> } })` (L402–447). Undo re-commits the pre-mutation snapshot atomically. |
| UC2 — Escape annulla ScalarChip edit (no commit, no toast) | artifact | PASS | `ScalarChip` (L1157–1222) uses `skipBlur` ref. Esc branch sets `skipBlur.current = true; onOpenChange(false);` (L1211–1214). `onBlur` checks `if (!skipBlur.current) submit();` (L1202–1205). No commit path reached → no toast. `autoFocus` on open and `draft` reset on `[value, open]` confirmed. |
| UC3 — Cambio tipo personaggio→evento, campi type-specific scartati, toast undo ripristina | artifact | PASS | `commitTipo` (L454–462) spreads `buildTypeResetPatch(nextTipo)` which clears `nascita/morte/tribu/ruoli/fazioni/esito/statoProfezia/dettagliRegno/regione` and, when target is non-personaggio, also `date` (L260–278). Toast label `"Tipo aggiornato"`, timeout 5s, `Annulla` re-normalizes `prevElement` (which still contains the personaggio-era fields) via `normalizeElementoInput(buildElementoInput(prevElement))` and re-commits. The `prevElement` snapshot is captured *before* `{ ...element, ...patch }`, so the ripristino is atomic. |
| UC4 — Editor esaustivo su 8 ElementoTipo | artifact | PASS | `TIPO_OPTIONS` enumerates all 8 (ElementoEditor.tsx:72–81). `normalizeElementoInput` (rules.ts:120–179) accepts fields for 5 type-owning variants (`personaggio/guerra/profezia/regno/luogo`) and returns OK with empty type-specific map for `evento/periodo/annotazione`. `buildElementoInput` switch default branch (L246) leaves `typeSpecific = {}` for evento/periodo/annotazione, adding `date` when `tipo ∈ {evento, periodo, profezia, regno}` (L223–227). Header renders a read-only date `Chip` for the date-owning non-personaggio tipi (L648–653). |
| UC5 — Validazione dominio: giorno=15 senza mese su evento | artifact | FAIL | Domain rule is correct: `validateDataStorica` returns `{ type: "day_requires_month" }` when `giorno` is set without `mese` (value-objects.ts:41–43), and `normalizeElementoInput` maps this to `data_non_valida` (rules.ts:155–161). **But the editor UI for a non-personaggio does not expose a date editor** — for `evento/periodo/regno/profezia` the header renders the date as a read-only `<Chip>` (ElementoEditor.tsx:648–653) with no popover or drawer. The UC's flow ("aprire il field `date` (drawer vita)") cannot be executed against the current UI surface. Domain correctness is established; UI affordance is missing. |
| UC6 — Tipo specifico non ammesso (tribù su evento) | artifact | PASS | `normalizeElementoInput` rules.ts:131–148 rejects cross-type fields with `tipo_specifico_non_ammesso`. Tests at `elemento.rules.test.ts:49,59` assert both "rejects tribu on a guerra" and "rejects nascita on an evento" with the correct error type. `commitPatch` surfaces the rejection via `setSurfaceError(error.type.replaceAll("_"," "))` (L444–446) and does not commit. |
| UC7 — Annotazioni mie vs altrui, tap "+ Aggiungi annotazione" crea Elemento | artifact | FAIL | **Steps 1–2 PASS**: `getAnnotazioniForElement` (display-helpers.ts:309–323) filters `ELEMENTI` where `tipo === "annotazione"` + linked, splits by `autore === currentAutore`, returns `{ mie, altreCount }`. `ElementoEditor` renders `annotazioni.mie` first and an informational line for `altreCount` (L809–828). Altrui annotations are not rendered as editable nodes → read-only constraint trivially holds. **Steps 3–4 FAIL**: there is no "+ Aggiungi annotazione" button in `ElementoEditor`. The only occurrence of that label is `src/ui/workspace-home/DetailPane.tsx:184` inside `DetailBody`, which is exported but no longer rendered (DetailPane renders `<ElementoEditor />` at line 241, DetailBody is dead code). Even that button is `isDisabled`. No creation flow exists for annotations. |
| UC8 — Soft delete con toast Annulla 30s + restore riseleziona id | artifact | PASS | `handleSoftDelete` (DetailPane.tsx:52–64) calls `softDeleteElement(elementId)` then `toast("… eliminato", { timeout: 30_000, actionProps: { children: "Annulla", onPress: () => restoreElement(elementId) } })`. `softDeleteElement` (workspace-ui-store.ts:129–137) appends to `deletedElementIds`, clears `selectedElementId`, closes editor, exits fullscreen. `restoreElement` (L143–149) removes id from `deletedElementIds` AND `workspaceUi$.selectedElementId.set(id)` — the detail pane will re-resolve and render the element. |
| UC9 — Soft delete senza undo: toast svanisce, elemento resta eliminato | artifact | PASS | HeroUI `toast` auto-dismisses after `timeout` (30s). `deletedElementIds` retains the id (no auto-removal), so the list-pane filter continues to hide it. `finalizeDelete` (workspace-ui-store.ts:156–161) is a documented no-op placeholder for Jazz persistence — consistent with the mock-data prototype scope. |
| UC10 — Aggiungi campo globale con menu contestuale su campi vuoti | artifact | PASS | `globalAddOptions` (ElementoEditor.tsx:570–576) builds the menu from 5 candidates (descrizione, tags, ruoli [personaggio-only], collegamenti-famiglia [personaggio-only], collegamenti-generici), filtered by per-field "visible" predicate (only shown if empty/admissible). The `Dropdown` renders only non-empty options; `onAction` calls `openFieldEditor(...)` which focuses the target field. When no options remain, the whole `Aggiungi campo` section is hidden. |
| UC11 — Filtro parentela robusto al reorder | artifact | PASS | Parentela filter is predicate-based, not index-based: `element.link.filter((l) => l.tipo === "parentela")` (ElementoEditor.tsx:354), `links.filter((link) => link.tipo === "parentela")` (L567), and `links.filter((link) => link.tipo !== "parentela")` for generics (L568). No slot leakage possible under array reorder. |
| UC12 — Fullscreen parity (stessa grammatica, stessa gerarchia, back preservato) | artifact | PASS | `FullscreenOverlay` (FullscreenOverlay.tsx:77–84) renders the same `<ElementoEditor element={…} editingFieldId={…} isFullscreen onDelete={() => handleSoftDelete(selectedElement)} />` — editor shell is identical. `editingFieldId` is read from the same store so inline edit + toast undo flows behave the same. Header has `<ArrowLeft/>` "Torna alla lista" + `<Minimize2/>` buttons (L47–73), both exiting fullscreen via `workspaceUi$.fullscreen.set(false)`. |
| Edge case — Elimina+Undo ripetuti lasciano lo stesso stato | artifact | PASS | `softDeleteElement` guards against duplicate id insertion (`if (!current.includes(id))`, workspace-ui-store.ts:131–133). `restoreElement` reverses deterministically. Idempotent. |
| Edge case — Blur con commit pending (race): second toast replaces first | artifact | NEEDS-HUMAN | HeroUI `toast()` without a sticky id generates a new toast per call, so visually the second supersedes the first. The undo action of the second toast captures `prevElement` at the moment of the second commit — i.e. the intermediate value between first and second mutation — so undoing the second toast restores the intermediate state, as the UC expects. Needs a live tap-through to confirm queue behavior. |
| Edge case — Cambio tipo con campi popolati, undo ripristina atomicamente | artifact | PASS | See UC3 notes — `prevElement` snapshot captures the full pre-change Elemento including all type-specific fields; `buildElementoInput` on the snapshot re-emits those fields and `normalizeElementoInput` succeeds because the pre-existing object was already valid; `commitNormalizedElement` re-applies all of them as a single override update (workspace-ui-store.ts:102–122). |
| Domain guardia compile-time `_exhaustive: never = tipo` (S02-SUMMARY claim) | artifact | NEEDS-HUMAN | `grep -n` for `_exhaustive` and `never` in `src/ui/workspace-home/ElementoEditor.tsx` returned no matches. `buildElementoInput` uses a plain `switch (next.tipo)` with `default: break` (L230–248), not the exhaustiveness-never idiom the summary claims. Runtime behavior for the 8 tipi is correct, but the compile-time guard mentioned in the summary is absent from the editor. Flagging as docs/impl mismatch — low severity because the behavior is asserted by tests, but worth addressing in a follow-up. |

## Overall Verdict

**PARTIAL** — 11 of 13 primary checks PASS on artifact inspection (UC1–UC4, UC6, UC8–UC12 plus 1 edge case). **UC5 and UC7 FAIL**: UC5 because the editor surface for non-personaggio date editing is not wired (domain validation is correct but unreachable from UI), and UC7 because the "+ Aggiungi annotazione" creation flow is not present in `ElementoEditor` (the only button with that label lives in dead `DetailBody` code and is `isDisabled`). 2 items marked NEEDS-HUMAN: race-condition toast queue behavior (live runtime only) and a minor S02-SUMMARY↔code mismatch on the exhaustiveness guard.

## Notes

- Lint + 78 tests pass end-to-end. The 3 "Failed Suites" come from stale `.gsd/worktrees/M001-S02/**` paths vitest still discovers after the worktree contents were unlinked; this matches the follow-up already flagged in `S02-SUMMARY.md` ("considerare aggiunta di `.gsd/worktrees` alle vitest `test.exclude`"). Not a regression.
- Gaps identified for follow-up (either a micro-slice before S02 is declared final, or a carry-over to an S02-followup / S03-polish track):
  1. **UC5 gap** — wire a date editor (drawer or popover) for `evento`/`periodo`/`regno`/`profezia`, so the rich `DataTemporale` domain (giorno/mese/anno + puntuale/intervallo) is actually editable. Today only `personaggio` gets the Vita drawer; others render a read-only chip.
  2. **UC7 gap** — add a "+ Aggiungi annotazione" affordance in `ElementoEditor`'s annotations section that creates a new `Elemento { tipo: "annotazione", autore: CURRENT_AUTORE }` linked bidirectionally to the current element. The wiring pattern already exists (soft-delete uses a similar session-override flow); what's missing is a create helper in `workspace-ui-store` and the button in `ElementoEditor`.
  3. **Exhaustiveness guard** — consider adding the `const _exhaustive: never = next.tipo` the summary references to `buildElementoInput` (or preferably to a helper exhaustive match in `elemento.rules.ts`), so the TS compiler catches future additions to `ElementoTipo` at build time instead of at the default branch.
  4. **Worktree cleanup** — add `'.gsd/worktrees/**'` to `vitest.config.ts` `test.exclude` (or the engine should clean up on checkpoint) so vitest doesn't discover stale paths post-merge.
- `.gsd/worktrees/M001-S02/` still exists on disk as an empty-ish residue (contains package.json, src, tests, vitest.config.ts, etc., but the actual test files are missing — hence the `Cannot find module` errors). The engine should remove it at the next checkpoint.

## Manual follow-up for human reviewer

The two FAIL items are implementation gaps, not verification failures. Please decide whether S02 can be accepted as-is (declaring UC5 date editor and UC7 annotation creation as explicit out-of-scope or S02-followup) or whether S02 needs a remediation pass to close them before moving on. The other 11 primary checks are fully satisfied by the committed code.
