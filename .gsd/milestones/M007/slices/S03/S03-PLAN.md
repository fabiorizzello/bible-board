# S03: Hook useFieldStatus + inline success + fix toast no-op

**Goal:** Introdurre l'hook `useFieldStatus` (stato machine idle→saving→success→idle con confronto prev/next) e integrarlo nei tre field con peso di ElementoEditor (InlineTitle, DescrizioneSection, TipoChip) in modo che: (a) blur senza modifica non produca NESSUNA notifica (R049 — fix toast no-op), (b) blur con modifica reale mostri un check inline fade-out 1.5s contestuale al campo (R050 — inline success feedback), e (c) la presentazione sia la stessa convenzione per tutti i field con peso (endContent per Input, overlay assoluto per Milkdown, adjacent per Chip-popover) — rispettando prefers-reduced-motion e usando solo transizioni opacity/transform.
**Demo:** Blur descrizione senza cambio non emette nulla; con cambio check fade-out 1.5s; chip tag check inline; select check accanto trigger

## Must-Haves

- Blur su titolo/descrizione/tipo senza alcuna modifica al valore NON invoca `commitPatch` (quindi nessun toast, nessuna notifica). Blur con modifica reale → `onCommit(prev, next)` invocato una sola volta, status transita a "success" e icona Check inline (opacità 100) appare contestuale al campo, poi fade-out a opacità 0 dopo 1500ms. Con `prefers-reduced-motion: reduce` il reset è immediato. Test unitari del hook coprono i 5 scenari chiave (no-op, change, transizione success, reset 1500ms, reduced-motion). Nessun uso di `observer()` HOC o `use$()`. Solo `opacity` e `transform` in transizioni. 126+ test pass, `tsc --noEmit` clean.

## Proof Level

- This slice proves: Unit: state machine del hook via `renderHook` + `act` con timer fake (`vi.useFakeTimers`). Integration: verifica strutturale via `rg` che il hook sia wired nei tre componenti e che `endContent`/overlay check icon esistano. La demo visiva (check fade-out) resta manual-QA su iPad — non è automatizzabile senza browser harness che S03 non introduce.

## Integration Closure

Il hook produce il contratto `onCommit(prev, next)` che S04 consumerà per `notifyMutation('update', ..., undoFn)`. Per non bloccare S04, l'interfaccia pubblica (firma, tipi, semantica no-op) è congelata in T01 e non deve cambiare nelle task successive di S03. Le chiamate esistenti a `toast(...)` dentro `commitPatch` rimangono intatte — saranno rimpiazzate da S04 quando il drawer sostituirà il canale toast.

## Verification

- L'hook non produce log (React puro). L'integrazione in ElementoEditor rimuove un canale rumoroso (toast su no-op) — il signal-to-noise del canale toast migliora: d'ora in poi ogni toast emesso rappresenta una mutazione reale. S04 (notification center) leggerà `onCommit(prev, next)` come trigger autorevole di mutazione persistita, quindi il contratto prev!==next del hook è boundary critica per la prossima slice.

## Tasks

- [x] **T01: Creare hook useFieldStatus con state machine e test unitari** `est:45-60 min`
  Creare il nuovo file `src/ui/workspace-home/useFieldStatus.ts` con un hook React puro e generico `useFieldStatus<T>(value: T, onCommit: (prev: T, next: T) => void)` che ritorna `{ status: 'idle'|'saving'|'success'|'error', onFocus, onBlur }`. Implementare la state machine descritta in S03-RESEARCH: onFocus cattura prev in un ref; onBlur(next) confronta prev vs next con uguaglianza stretta (===); se uguali non chiama onCommit e lo status resta idle (FIX no-op R049); se diversi chiama onCommit(prev, next) e transita a 'saving' → 'success'; dopo 1500ms lo status torna a idle via setTimeout (cancellabile). Rispettare `prefers-reduced-motion: reduce` via `window.matchMedia` letto al momento del fire del timer (non a render time) — se attivo, reset immediato. Sincronizzare prev con value esterno via useEffect quando il parent aggiorna value (Jazz re-render). Export nominato. Nessun import da HeroUI, Jazz, Legend State. Contestualmente creare `src/ui/workspace-home/__tests__/useFieldStatus.test.ts` con @testing-library/react `renderHook` + `act` + `vi.useFakeTimers()` che copra: (1) onBlur con stesso valore non chiama onCommit e status resta idle; (2) onBlur con valore diverso chiama onCommit(prev, next) una sola volta; (3) status transita idle→success dopo change; (4) status torna a idle dopo 1500ms; (5) prefers-reduced-motion mockato (window.matchMedia mock) → reset immediato senza attendere 1500ms; (6) focus senza blur non chiama onCommit (idempotency). Il hook è puro — no dipendenze con il resto del codice, testabile in isolamento.
  - Files: `src/ui/workspace-home/useFieldStatus.ts`, `src/ui/workspace-home/__tests__/useFieldStatus.test.ts`
  - Verify: pnpm test --run src/ui/workspace-home/__tests__/useFieldStatus.test.ts (tutti i 5-6 scenari pass); pnpm tsc --noEmit (clean); wc -l src/ui/workspace-home/useFieldStatus.ts ≥30; grep -n 'export function useFieldStatus' src/ui/workspace-home/useFieldStatus.ts (1 match); rg 'observer\(|use\$\(' src/ui/workspace-home/useFieldStatus.ts (0 match).

- [ ] **T02: Wire useFieldStatus in InlineTitle con check icon in endContent** `est:30-45 min`
  Modificare `InlineTitle` (attualmente intorno alla riga 959 di `src/ui/workspace-home/ElementoEditor.tsx` — verificare la posizione attuale con rg/grep prima di editare, non affidarsi ai numeri di riga statici). Dentro InlineTitle: (1) importare `useFieldStatus` da `./useFieldStatus`; (2) importare `Check` da `lucide-react` (già usato altrove nel file — verificare import esistente); (3) istanziare `const { status, onFocus, onBlur } = useFieldStatus<string>(value, (prev, next) => onCommit(next));` — adattare la firma al contratto corrente di `InlineTitle` (il callback parent si aspetta un solo argomento `next`); (4) sostituire `onBlur={() => onCommit(draft)}` del `<Input>` con `onBlur={() => onBlur(draft)}` e aggiungere `onFocus={onFocus}`; (5) aggiungere al `<Input>` la prop `endContent` che renderizza `<Check className='h-4 w-4 transition-opacity duration-300 opacity-0' aria-hidden='true' style={{ opacity: status === 'success' ? 1 : 0 }} />` — usare SOLO opacity, mai width/height. NON modificare `commitPatch` né rimuovere toast esistenti: il contratto no-op nasce dal fatto che `onCommit` non viene più chiamato su valori identici. Verificare che, se draft === value al blur, il parent onCommit non venga invocato (test manuale rapido o test di snapshot opzionale).
  - Files: `src/ui/workspace-home/ElementoEditor.tsx`
  - Verify: pnpm test --run (126+ pass); pnpm tsc --noEmit (clean); rg -n 'useFieldStatus' src/ui/workspace-home/ElementoEditor.tsx (≥1 hit per T02, crescerà nei task successivi); rg -n 'endContent.*Check|Check.*endContent' src/ui/workspace-home/ElementoEditor.tsx | head (match nella zona InlineTitle); rg 'transition.*width|transition.*height|animate.*width|animate.*height' src/ui/workspace-home/ElementoEditor.tsx (0 match — solo opacity/transform ammessi).

- [ ] **T03: Wire useFieldStatus in DescrizioneSection con check icon overlay assoluto** `est:30-45 min`
  Modificare `DescrizioneSection` (attualmente intorno alla riga 1278 di `src/ui/workspace-home/ElementoEditor.tsx` — verificare posizione corrente). La descrizione usa Milkdown che non espone un input nativo; il blur è gestito a livello container div. Dentro DescrizioneSection: (1) istanziare `const { status, onFocus, onBlur } = useFieldStatus<string>(value, (prev, next) => onCommit(next));` (adattare al callback esistente); (2) sostituire `handleBlur` (che chiama `onCommit(draft)`) con `handleBlur = () => onBlur(draft)` e aggiungere un `onFocus={onFocus}` sul container wrapper del milkdown-host; (3) posizionare il container editor in `relative` (se non lo è già) e aggiungere un `<Check className='absolute bottom-2 right-2 h-4 w-4 pointer-events-none transition-opacity duration-300' aria-hidden='true' style={{ opacity: status === 'success' ? 1 : 0 }} />`. Nessun cambio al comportamento di submit del Milkdown. Il valore `draft` catturato allo stato del componente al momento del blur è già sufficiente per il confronto prev/next.
  - Files: `src/ui/workspace-home/ElementoEditor.tsx`
  - Verify: pnpm test --run (126+ pass); pnpm tsc --noEmit (clean); rg -c 'useFieldStatus' src/ui/workspace-home/ElementoEditor.tsx (≥2 hit dopo T02+T03); rg -n 'absolute.*bottom.*right|bottom.*right.*absolute' src/ui/workspace-home/ElementoEditor.tsx (≥1 hit in zona DescrizioneSection); rg 'transition.*width|transition.*height' src/ui/workspace-home/ElementoEditor.tsx (0 match).

- [ ] **T04: Wire no-op guard + check icon adjacent in TipoChip** `est:30-45 min`
  Modificare `TipoChip` (attualmente intorno alla riga 1077 di `src/ui/workspace-home/ElementoEditor.tsx`). TipoChip commette su press di un option del popover, non su blur — il pattern onFocus/onBlur del hook non si applica direttamente. Due strade possibili documentate nella research; scegliere la più semplice: (1) aggiungere un guard inline: `const handleSelect = (option: ElementoTipo) => { if (option === tipo) { closePopover(); return; } onCommit(option); }` — questo fixa R049 per il tipo; (2) per il feedback inline success usare un local state `const [justCommitted, setJustCommitted] = useState(false)` impostato a true subito dopo onCommit, e resettato via setTimeout 1500ms (con rispetto di prefers-reduced-motion tramite `window.matchMedia` al fire del timer — stessa logica del hook). Renderizzare `<Check className='ml-2 h-4 w-4 transition-opacity duration-300' aria-hidden='true' style={{ opacity: justCommitted ? 1 : 0 }} />` adiacente al `<ChipButton>` trigger. Se la scelta è di usare `useFieldStatus` anche qui, documentare come value si passa `tipo` (prop corrente) e onFocus viene chiamato alla popover-open; ma l'approccio con local state è più diretto e produce lo stesso UX. Decidere in base a quale produce meno codice — il contratto R050 richiede il check inline, non specifica l'implementazione.
  - Files: `src/ui/workspace-home/ElementoEditor.tsx`
  - Verify: pnpm test --run (126+ pass); pnpm tsc --noEmit (clean); rg -n 'option === tipo|option !== tipo' src/ui/workspace-home/ElementoEditor.tsx (≥1 hit — guard no-op presente); grep in zona TipoChip di `<Check` e `justCommitted` oppure `status === 'success'`; rg 'transition.*width|transition.*height' src/ui/workspace-home/ElementoEditor.tsx (0 match). Smoke manuale: selezionare lo stesso tipo già attivo non deve chiamare commitPatch (non appare toast).

## Files Likely Touched

- src/ui/workspace-home/useFieldStatus.ts
- src/ui/workspace-home/__tests__/useFieldStatus.test.ts
- src/ui/workspace-home/ElementoEditor.tsx
